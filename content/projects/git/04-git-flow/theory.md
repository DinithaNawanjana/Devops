# Project: Feature-branch Flow

A lightweight Git Flow: integrate work on `develop`, build each feature on a
`feature/*` branch, and merge it back with `--no-ff` so the history shows the
feature boundary.
